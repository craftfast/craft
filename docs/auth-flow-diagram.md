# Authentication Flow Diagram

## 🎯 Complete Authentication Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CRAFT.TECH                              │
│                    (Single Domain App)                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  User Visits /  │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
              ▼                             ▼
     ┌────────────────┐           ┌────────────────┐
     │ Not Logged In  │           │   Logged In    │
     └────────┬───────┘           └────────┬───────┘
              │                             │
              ▼                             ▼
     ┌─────────────────────┐      ┌──────────────────────┐
     │  LANDING PAGE (/)   │      │   DASHBOARD (/)      │
     ├─────────────────────┤      ├──────────────────────┤
     │ • Hero Section      │      │ • User Projects      │
     │ • Features          │      │ • Recent Activity    │
     │ • Waitlist Form     │      │ • Credits/Usage      │
     │ • [Sign In]         │      │ • Quick Actions      │
     │ • [Sign Up]         │      │ • [Profile Menu]     │
     └──────────┬──────────┘      └──────────┬───────────┘
                │                             │
                ▼                             │
     ┌─────────────────────┐                 │
     │ Click "Sign Up"     │                 │
     └──────────┬──────────┘                 │
                │                             │
                ▼                             │
     ┌─────────────────────┐                 │
     │  /auth/signup       │                 │
     ├─────────────────────┤                 │
     │ Choose Auth Method: │                 │
     │ ┌─────────────────┐ │                 │
     │ │ Google OAuth    │ │                 │
     │ └─────────────────┘ │                 │
     │ ┌─────────────────┐ │                 │
     │ │ GitHub OAuth    │ │                 │
     │ └─────────────────┘ │                 │
     │ ┌─────────────────┐ │                 │
     │ │ Email+Password  │ │                 │
     │ └─────────────────┘ │                 │
     └──────────┬──────────┘                 │
                │                             │
                ▼                             │
     ┌──────────────────────┐                │
     │  Account Created     │                │
     │  ✅ User in DB       │                │
     │  ✅ Session Created  │                │
     └──────────┬───────────┘                │
                │                             │
                └─────────────────────────────┘
                              │
                              ▼
                    ┌───────────────────┐
                    │ Redirect to /     │
                    │ (Shows Dashboard) │
                    └───────────────────┘
```

## 📊 Authentication Methods

### 1. Google OAuth Flow

```
User clicks "Continue with Google"
        ↓
Redirect to Google consent screen
        ↓
User approves
        ↓
Callback to /api/auth/callback/google
        ↓
NextAuth creates/finds user
        ↓
Session created
        ↓
Redirect to / (Dashboard)
```

### 2. GitHub OAuth Flow

```
User clicks "Continue with GitHub"
        ↓
Redirect to GitHub authorization
        ↓
User approves
        ↓
Callback to /api/auth/callback/github
        ↓
NextAuth creates/finds user
        ↓
Session created
        ↓
Redirect to / (Dashboard)
```

### 3. Email+Password Flow

```
User fills signup form
        ↓
Submit to /api/auth/register
        ↓
Password hashed with bcrypt
        ↓
User created in database
        ↓
Auto sign-in with credentials
        ↓
Session created
        ↓
Redirect to / (Dashboard)
```

## 🔄 Session Management

```
┌──────────────────────────────────────┐
│         User Session (JWT)           │
├──────────────────────────────────────┤
│ • Stored in httpOnly cookie          │
│ • Expires in 30 days (default)       │
│ • Contains user ID                   │
│ • Automatically refreshed            │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│      Server Components (RSC)         │
│  const session =                     │
│    await getServerSession()          │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│      Client Components               │
│  const { data: session } =           │
│    useSession()                      │
└──────────────────────────────────────┘
```

## 🗄️ Database Schema

```
┌─────────────────────┐
│       users         │
├─────────────────────┤
│ id (PK)             │
│ email (unique)      │
│ name                │
│ password (hashed)   │ ← Only for email/password auth
│ emailVerified       │
│ image               │
│ createdAt           │
│ updatedAt           │
└──────────┬──────────┘
           │
           │ 1:N
           ▼
┌─────────────────────┐
│      accounts       │ ← OAuth providers
├─────────────────────┤
│ id (PK)             │
│ userId (FK)         │
│ provider            │ ← "google" | "github"
│ providerAccountId   │
│ access_token        │
│ refresh_token       │
│ expires_at          │
└─────────────────────┘
           │
           │ 1:N
           ▼
┌─────────────────────┐
│      sessions       │
├─────────────────────┤
│ id (PK)             │
│ userId (FK)         │
│ sessionToken        │
│ expires             │
└─────────────────────┘
```

## 🚦 Route Protection (Next Steps)

```
middleware.ts (to be added)
        ↓
┌──────────────────────────────────────┐
│  Protected Routes:                   │
│  • /dashboard  → Requires auth       │
│  • /profile    → Requires auth       │
│  • /settings   → Requires auth       │
│  • /projects   → Requires auth       │
│                                      │
│  Public Routes:                      │
│  • /            → Smart (conditional)│
│  • /home        → Always public      │
│  • /pricing     → Public             │
│  • /help        → Public             │
│  • /auth/*      → Public             │
└──────────────────────────────────────┘
        ↓
┌──────────────────────────────────────┐
│  Not authenticated + protected route │
│         ↓                            │
│  Redirect to /auth/signin            │
│  with callbackUrl parameter          │
└──────────────────────────────────────┘
```

## 🎨 UI Components Hierarchy

```
RootLayout
  └─ SessionProvider (client)
      ├─ HeaderNav
      │   ├─ Logo
      │   ├─ Navigation (conditional)
      │   └─ UserMenu (if authenticated)
      │
      ├─ Page Content
      │   ├─ Landing Page (if not authenticated at /)
      │   │   ├─ Hero
      │   │   ├─ Features
      │   │   └─ WaitlistForm
      │   │
      │   └─ Dashboard (if authenticated at /)
      │       ├─ Welcome
      │       ├─ Projects
      │       └─ Quick Actions
      │
      └─ Footer
```

## 🔐 Security Layers

```
┌──────────────────────────────────────────┐
│         Application Security             │
└──────────────────────────────────────────┘
         │
         ├─ 1. Password Hashing
         │    └─ bcrypt (12 salt rounds)
         │
         ├─ 2. Session Security
         │    ├─ JWT tokens
         │    ├─ httpOnly cookies
         │    └─ CSRF protection
         │
         ├─ 3. OAuth Security
         │    ├─ OAuth 2.0 standard
         │    ├─ Secure callbacks
         │    └─ Provider verification
         │
         ├─ 4. Database Security
         │    ├─ Prisma ORM (SQL injection protection)
         │    ├─ SSL connections
         │    └─ Environment variables
         │
         └─ 5. API Security
              ├─ Input validation
              ├─ Error handling
              └─ Rate limiting (to be added)
```

## 📱 User Experience Flow

```
New User Journey:
┌──────────────────────────────────────────┐
│ 1. Lands on craft.tech/                 │
│    → Sees marketing content              │
│    → Clicks "Sign Up"                    │
└───────────────┬──────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────┐
│ 2. On /auth/signup                       │
│    → Chooses auth method                 │
│    → Creates account                     │
└───────────────┬──────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────┐
│ 3. Redirected to craft.tech/             │
│    → Same URL, different content!        │
│    → Now sees Dashboard                  │
└───────────────┬──────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────┐
│ 4. Explores app                          │
│    → Can access /home for landing        │
│    → Can go to /pricing                  │
│    → Can manage /settings                │
└──────────────────────────────────────────┘

Returning User Journey:
┌──────────────────────────────────────────┐
│ 1. Visits craft.tech/                    │
│    → Session cookie still valid          │
│    → Immediately sees Dashboard          │
└───────────────┬──────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────┐
│ 2. Continues using app                   │
│    → All features accessible             │
│    → Session persists across refreshes   │
└──────────────────────────────────────────┘
```

---

## 🎯 Current Status

✅ **COMPLETED:**

- Database schema with Prisma
- NextAuth configuration
- Auth API routes
- Sign-in page
- Sign-up page
- Error page
- Session provider
- TypeScript types
- Documentation

⏳ **PENDING:**

- Update root page to check auth
- Create Dashboard component
- Update HeaderNav for auth state
- Add middleware for protection
- Test all flows end-to-end

---

**The foundation is rock solid! Ready to connect the final pieces.** 🚀
